# Python Mini Projects 🐍

Welcome to my repository for CLI-based Python mini-projects built during my learning journey.

## 🚀 Projects Included
1. **Number Guessing Game** (`modify guessing game .py`)
   - Uses `while` loops, conditional logic (`if-elif-else`), and user input handling.
   - Generates random numbers using Python's `random` module.

## 🛠️ How to Run
1. Clone or download this repository.
2. Open terminal/command prompt in the file directory.
3. Run the Python script:
   ```bash
   python "modify guessing game .py"
